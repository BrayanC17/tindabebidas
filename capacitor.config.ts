import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brayan.tindabebidas',
  appName: 'tindabebidas',
  webDir: 'dist/tindabebidas/browser'
};

export default config;
